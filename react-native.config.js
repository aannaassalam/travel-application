module.exports = {
  project: { ios: {}, android: {} },
  // Bundled fonts: filenames match their PostScript names, which is what makes
  // iOS (PostScript lookup) and Android (filename lookup) agree on fontFamily.
  assets: ["./assets/fonts"]
};
