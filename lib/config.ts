/** Where the app points — production API for all builds. */
export const API_BASE = "https://backend.flexiairbnb.com/api/v1";

/** The website, for the "view on the site" escape hatches. */
export const SITE_ORIGIN = "https://flexiairbnb.com";
